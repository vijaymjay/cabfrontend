
Please follow the below instructions to start angular application.

1. If we want to change the API url, goto src/environments/environments.js and src/environments/environments.prod.js. Here we can set the server url to connect webservice.

2. Open the cmd in root path of the angular application and write npm install command to install dependencies of the application.

3. Type ng serve command to start the application.